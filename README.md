# sql-even-odd-employee-filter
SQL queries to filter employees based on even and odd employee IDs using the modulo operator.
# SQL Even & Odd Employee Filter

## 📌 Overview
This repository demonstrates how to filter employee records based on whether the `emp_id` is even or odd using SQL.

The logic is commonly used in:
- Data validation
- Batch processing
- Load balancing
- Interview questions

---

## 🛠 SQL Logic Used
The modulo (`%`) operator is used to determine whether a number is even or odd.

- **Even Number** → remainder is `0`
- **Odd Number** → remainder is `1`

---


 
